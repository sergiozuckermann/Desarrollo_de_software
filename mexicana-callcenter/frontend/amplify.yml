version: 1
applications:
  - frontend:
      frontend:
        phases:
          build:
            commands:
              - cd frontend
              - npm install  # Instala las dependencias necesarias
              - npm run build  # Compila la aplicación frontend para producción
        artifacts:
          baseDirectory: frontend/build  # Directorio donde se genera la compilación
          files:
            - '**/*'
        cache:
          paths:
            - 'frontend/node_modules/**/*'

# Puedes añadir secciones adicionales para otras partes del monorepo si es necesario
# - backend:
#     ...

# - lambdas:
#     ...
