version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install react-tooltip-lite --legacy-peer-deps
    build:
      commands:
        - npm run dev  # o npm run build según corresponda
  artifacts:
    baseDirectory: mexicana-callcenter/frontend
    files:
      - '**/*'
  cache:
    paths:
      - 'node_modules/**/*'
